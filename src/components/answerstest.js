import React from 'react';

export default (props)=>{
    const {wrongAnswers}=props;
    const {correctAnswer}=props;
    
    const wrongDivs = wrongAnswers.map( (item, index)=>{
        
    } )


    return(
        <div>
            <div  > {correctAnswer} </div>

        </div>
    )
}